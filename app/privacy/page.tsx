const Privacy = async () => {
  return (
    <main>
      <p>
        We wont use your data for any other purpose than to run this application. As such, we delete your data if you
        request. Additionally we only ask to store the data required to run this application.
      </p>
    </main>
  )
}
export default Privacy
